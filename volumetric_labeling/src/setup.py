from setuptools import setup
setup(name='volumetric_labeling',
      version='1.0',
      packages=[
          'volumetric_labeling'
          'volumetric_labeling.labels',
          'volumetric_labeling.utils'
      ])
